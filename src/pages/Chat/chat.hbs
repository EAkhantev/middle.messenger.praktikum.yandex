<div class="wrapper">
  <aside class="side-bar">
    {{{ chatNav }}}
    <ul class="contact-list">
      {{#each contactItems}}
        {{{ this }}}
      {{/each}}
    <ul>
  </aside>
  <div class="chat-area">
    Выберите чат чтобы отправить сообщение
  </div>
</div>
